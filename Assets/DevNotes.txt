DevNotes

Pending:
 - Assign accessory to toggleItem before subscribing the event onValueChanged en CategorySectionView
 - Use the DogAccessorySO prefab in TabsItemSelector to make references directly to the prefabs which already have a color assigned (example redcap) ---> maybe we can use DogAccessoryToggleItem.Init which instantiates the object using the right prefab, in that case I should override the instantiation on TabsItemSelector code


Changes to game code:
 - PreparationStation:
	- SelectAccessoryOption set s public method to be able to call it from the Tabs Selector code, specifically from CategorySectionView.AssignToggleGroupNextFrame(ItemCellView)


Abstract class

Review:
 - Content size fitter order creation ahora en UIBuilder

Pending:
- Add tabs functionality
  -- Buttons:
    -- Calculate contents size
    -- Jump to first category item


Future:
- Manage sections/tabs equally:
 -- Create a list of UIItemsContent that takes all parameters:
   -- LayoutGroupType (grid, vertical, horizontal)
   -- bool hasMultipleSections. 
   
  *-- Allows to:
     -- Add all categories in a GridLayout without line shifts when each category finishes. (If hasMultipleSections = false)
     -- Add multiple rows of tabs and treat them as a GridLayout if needed
   
- Make it so if tabs or ítems can set a min size
  -- Able to add LayoutElement to tabs or ítems to set preferred or max/min sizes
    -- Set content Size Fitter to actívate only if total size of objects demands it
